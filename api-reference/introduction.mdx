---
title: "API Overview"
description: "Get started with our developer-first, production-ready REST API for building scalable platforms."
---

<Note>
  💡 This is your launchpad to the API universe. If you're not building on the
  API, feel free to remove this section by deleting the `api-reference` folder.
</Note>

# 🧠 Welcome to the [YourCompany] API

**Build. Automate. Scale.**  
The **[YourCompany] API** unlocks the full power of our platform — giving you secure access to users, projects, files, auth flows, and more. This RESTful interface is designed for developers who want precision, performance, and flexibility out of the box.

---

## 🌐 Environments

| Type       | Base URL                                 |
| ---------- | ---------------------------------------- |
| Production | `https://api.yourcompany.com/v1`         |
| Staging    | `https://staging-api.yourcompany.com/v1` |
| Sandbox    | `https://sandbox-api.yourcompany.com/v1` |

> 🧪 Use the **Sandbox** for safe testing with dummy data, no billing risk.

---

## 🔐 Authentication

All endpoints (unless noted otherwise) require a **JWT Bearer Token** in the `Authorization` header.

```http
Authorization: Bearer <your_jwt_token>
```
